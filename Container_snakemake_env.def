Bootstrap: docker
From: condaforge/miniforge3:latest

%files
    prebuilt-envs /conda-envs

%post
    conda create -y -n snakemake_env -c conda-forge -c bioconda snakemake snakemake-storage-plugin-fs fastp fastqc minimap2 multiqc samtools
    conda clean --all -y

    